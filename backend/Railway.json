{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "restartPolicyType": "NEVER",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10000,
    "startCommand": "node server.js"
  }
}
